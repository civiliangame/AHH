# Local secrets — gitignored. ROTATE this key: it was shared in plaintext.
XAI_API_KEY=xai-PLACEHOLDER_ROTATED

XAI_REALTIME_URL=wss://api.x.ai/v1/realtime


HOST=0.0.0.0
PORT=5000
LOG_LEVEL=INFO

# Telnyx Call Control. ROTATE this key too — it was shared in plaintext.
TELNYX_API_KEY=KEY_PLACEHOLDER_ROTATED

PUBLIC_HOSTNAME=https://thoroughly-liberal-mouse.ngrok-free.app
STREAM_PATH=/media-stream

# Outbound calls (run_checkin.py): Call Control App connection id + a Telnyx
# number on it to call from. Both from the Telnyx portal.
TELNYX_CONNECTION_ID=2981555662628062476
TELNYX_FROM_NUMBER=+18188673978

# --- DAM (post-call depression/anxiety scoring; see analysis.py) ---
# Path to the trained checkpoint (default: ./dam/dam3.1.ckpt)
# DAM_CHECKPOINT=dam/dam3.1.ckpt
# Skip scoring for calls shorter than this many seconds (default 5).
# DAM_MIN_SECONDS=5

# --- Clinician summary (summary.py) ---
# Salt for hashing phone numbers into opaque URL/file IDs. Rotate to invalidate
# all existing summary URLs. Required for the /api/patients endpoints.
SUMMARY_SALT=dev-only-rotate-me